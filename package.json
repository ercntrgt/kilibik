{
  "name": "kilibik",
  "private": true,
  "version": "0.1.0",
  "description": "Çiftler için karşılıklı istek listesi, anlık konum ve dürtme uygulaması — sunucu kördür.",
  "workspaces": [
    "shared",
    "server"
  ],
  "scripts": {
    "migrate": "npm run migrate -w server",
    "dev:server": "npm run dev -w server",
    "test": "npm test -w server",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck --prefix mobile",
    "redlines": "npm run test:redlines -w server"
  },
  "engines": {
    "node": ">=20"
  }
}
